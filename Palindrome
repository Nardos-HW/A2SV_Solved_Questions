class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """

        y = str(x)
        z = y[::-1]

        if (y == z):
            return True
        return False

"""
or just:-

return str(x) == str(x)[::-1]

"""
