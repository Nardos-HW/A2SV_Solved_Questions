class Solution(object):
    def missingNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        for i in range(len(nums) + 1):
            if i not in nums:
                return i

"""
also:-
n = len(nums)
expected_sum = n * (n+1) // 2
sums = sum(nums)
missing_num = expected_sum - sums
return missing_num
"""
